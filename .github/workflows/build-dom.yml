name: Build

on:
  push:
  pull_request:
    branches: [ "main" ]
    paths: 
    - 'src/Reveal.Sdk.Dom.sln'
    - 'src/Reveal.Sdk.Dom/**'
    - 'src/Reveal.Sdk.Dom.Tests/**'

jobs:
  build-dom:
    uses: ./.github/workflows/build
